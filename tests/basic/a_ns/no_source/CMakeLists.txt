
# This target compiles:
# as no src directory exists, we create it - includng an empty file, and as
# this is an executable, a dummy main cpp with a main function

ccf_ns("no_source")
ccf_target(ExECutaBLE)
