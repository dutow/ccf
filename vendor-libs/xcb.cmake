# Finds XCB includes and libraries

INCLUDE_GUARD()

IF( CANCELLAR_PLATFORM_LINUX )
  FIND_PACKAGE( XCB REQUIRED )
  LIST( APPEND CANCELLAR_VENDOR_INCLUDES "${XCB_INCLUDE_DIR}" )
  LIST( APPEND CANCELLAR_VENDOR_LIBRARIES "${XCB_LIBRARIES}" )
ENDIF()

